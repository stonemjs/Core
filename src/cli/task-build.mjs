/**
 * Build task.
 *
 * @param   {IncomingEvent} event
 * @param   {Container} container
 * @returns {void}
 */
export const buildTask = async (event, container) => {
  console.log('Build taks', event)
}
