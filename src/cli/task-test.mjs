/**
 * Test task.
 *
 * @param   {IncomingEvent} event
 * @param   {Container} container
 * @returns {void}
 */
export const testTask = async (event, container) => {
  console.log('Test taks', event)
}
